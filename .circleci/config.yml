test-defaults: &test-defaults
  docker:
    - image: quay.io/pantheon-public/build-tools-ci:2.x
  working_directory: ~/cwidahocc/d8cwi
  environment:
    TZ: "/usr/share/zoneinfo/America/Los_Angeles"
    TERM: dumb
    CIRCLE_TEST_REPORTS: /tmp/circleci-test-results
    CIRCLE_ARTIFACTS: /tmp/circleci-artifacts

version: 2
jobs:
  build:
    <<: *test-defaults
    shell: /bin/bash --login
    steps:
      - checkout
      - run: echo "hello world"
      - run: mkdir -p $CIRCLE_ARTIFACTS $CIRCLE_TEST_REPORTS
      - run: 
          name: TimeZone and Sudo
          command: echo ''America/Boise'' | tee -a /etc/timezone; dpkg-reconfigure -f noninteractive tzdata; service mysql restart; service postgresql restart; 